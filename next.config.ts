/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['localhost'], // Adicione outros domínios se estiver usando imagens externas
  },
};

module.exports = nextConfig;